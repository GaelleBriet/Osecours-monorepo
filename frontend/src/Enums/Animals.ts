export enum AnimalStatus {
	found = 1,
	available = 2,
	// hosted,
	foster_family = 3,
	adopted = 4,
	deceased = 5,
	under_observation = 6,
	lost = 8,
}

export enum AnimalSpecies {
	Cat = 1,
	Dog = 2,
}

export enum AnimalGenders {
	male = 1,
	female = 2,
	unknown = 3,
}

export enum AnimalSizes {
	small = 1,
	medium = 2,
	large = 3,
	molosse,
}

export enum AnimalAges {
	baby = 1,
	young = 2,
	adult = 3,
	senior = 4,
}
